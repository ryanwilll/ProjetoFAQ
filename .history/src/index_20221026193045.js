const faqs = document.querySelectorAll(".text");


faqs.forEach((faq) => {
  faq.addEventListener("click", () => {
    if ('.text.active' >= 1){
      getSelection.
    }
    
    faq.classList.toggle("active");
  });
});
